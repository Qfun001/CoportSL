# CoportSL 桌面端使用说明

[English](USER_GUIDE.md) | 简体中文

$\quad \quad$本说明面向使用 Windows 打包版的用户。希望编译、修改或参与开发 CoportSL 的读者可参阅[项目 README](README-zh.md#从源码构建)。

## 1. 下载与启动

1. 打开 [CoportSL Releases 页面](https://github.com/Qfun001/CoportSL/releases)。
2. 进入需要的版本，在 **Assets** 区域下载 `CoportSL.exe`。页面中的源码压缩包不含已构建的应用程序。
3. 可以将文件的 SHA-256 校验和与发布说明中的数值进行比对。
4. 把 `CoportSL.exe` 放入可写目录并双击运行。该程序采用单文件便携形式，无需另行安装 Python 或 Visual Studio。

$\quad \quad$首次启动时，单文件程序需要解压运行组件，窗口可能等待数秒后出现。CoportSL 当前未使用商业代码签名证书，Windows SmartScreen 可能显示“未知发布者”。请确认文件来自官方 Releases 页面且校验和一致，再按需选择“更多信息 > 仍要运行”。

$\quad \quad$CoportSL 优先在可执行文件旁的 `CoportSL-data` 目录保存设置、作业描述、状态和日志；该位置不可写时，程序会改用当前 Windows 用户的应用数据目录。“设置”页会显示实际使用的路径。科研输入与结果目录由用户单独选择，不会写入可执行文件。

## 2. 准备输入数据

$\quad \quad$应用程序不包含 GRMHD 模拟数据。当前支持的 BHAC 后端需要以下内容：

- 一个固定 SMR 帧序列目录，文件名采用 `dataNNNN.dat`，例如 `data1200.dat` 和 `data1201.dat`；
- 与该帧序列匹配的静态网格文件 `grid_mks.in`；
- 一个具有足够可用空间的结果目录，用于保存 Stokes CSV、分析记录、图件和视频。

$\quad \quad$所有帧必须使用相同的网格和 primitive 变量布局，帧头时刻应严格递增并保持等间隔。条件允许时可将原始数据和网格设为只读；CoportSL 仅在用户选择的结果目录下写入科研输出。

## 3. 首次设置

$\quad \quad$打开“数据与模型”，依次选择 BHAC 数据目录、静态网格文件和结果根目录。程序会自动发现可用帧范围与时间间隔。随后检查真实源、成像屏、观者位置、光线积分、电子分布及慢光分析参数。

$\quad \quad$开始长时间计算前，先点击“检查当前设置”。检查通过表示当前字段可以组成有效作业，且程序能够读取所选输入。实际任务能否在现有内存和磁盘空间内完成，仍取决于图像分辨率、帧数和具体参数。

$\quad \quad$全新安装默认显示英文。需要简体中文时，打开“Settings”，在“Language”中选择“简体中文”；界面会即时更新并自动保存选择。

## 4. 界面页面

| 页面 | 用途 |
| --- | --- |
| “数据与模型” | 选择输入输出路径，设置共用的物理、成像屏、观者、光线、电子分布、空间分区及分析参数。 |
| “Flux 定标” | 在选定帧上测量平均光通量，并给出吸积率试算建议。 |
| “正式计算” | 执行快光成像、慢光前置分析与成像，以及区域截断误差计算。 |
| “后处理” | 发现完整结果，生成积分观测量、EVPA 图件、对比曲线、科研图件或视频。 |
| “GRMHD 工具” | 绘制 BHAC 物理量和剖面，生成时间序列或视频，并检查插值行为。 |
| “性能基准” | 测量不同图像分辨率和 CPU 核心组下的性能。 |
| “运行记录” | 查看作业状态并打开以往任务的完整日志。 |
| “设置” | 切换语言和主题，调整日志容量与历史保留数量，查看应用数据路径。 |

$\quad \quad$当前页面上方的操作栏提供通用按钮。“检查当前设置”用于校验页面，“运行当前任务”用于提交作业，“打开结果目录”用于打开所选结果根目录。在“正式计算”页还可以单独执行“慢光前置分析”。

## 5. 推荐计算流程

### 第一步：Flux 定标

$\quad \quad$打开“Flux 定标”，选择具有代表性的闭区间帧范围、采样步长和一个或多个频率，并填写目标光通量密度。任务完成后查看平均光通量和建议吸积率。建议值用于下一次试算；修改吸积率后应重新运行 Flux，因为吸收与法拉第项也会随归一化发生变化。

### 第二步：生成快光参考结果

$\quad \quad$打开“正式计算”并选择“快光”。可以保留自动完整帧范围，也可以启用较小的手动范围进行试算。完成校验并运行作业。完整快光结果可供后续时间对齐、快慢光比较和区域误差计算使用。

### 第三步：执行慢光前置分析

$\quad \quad$在“正式计算”页运行“慢光前置分析”。分析会评估所选空间分区内各类转移系数的贡献范围，并记录建议慢光区域与有效时间窗口。输入内容、共用模型参数、区域定义或分析容限发生变化时，分析签名也会变化，通常需要生成新的匹配分析。

### 第四步：生成慢光图像

$\quad \quad$选择“慢光”。“自动采用分析建议”会使用兼容分析给出的区域；“手动选择命名集合”会使用“数据与模型”页定义的集合。选择 `p99` 等时间窗口，检查设置后运行。自动模式找不到兼容分析时，作业会先完成前置分析，再继续成像。

### 第五步：检查误差并生成图件

$\quad \quad$需要量化命名集合的区域截断误差时，可运行“区域误差”。生成常规图件和派生观测量时，打开“后处理”，选择一个完整结果，勾选所需任务与输出格式，然后运行。快慢光比较要求两组结果完整且相互兼容；页面会显示发现的签名或参数差异。

### 第六步：使用可选工具

$\quad \quad$“GRMHD 工具”可以生成模拟平面图、剖面、吸积率或磁通量时间序列、primitive 变量插值检查及视频。“性能基准”会在选定分辨率和 CPU 核心组上重复测量。这些任务可能占用较多时间、内存、存储空间和处理器资源，建议先使用较小帧范围试运行。

## 6. 作业、日志、取消与结果

$\quad \quad$同一时刻只会调度一个作业。任务抽屉显示当前状态和近期输出。取消采用协作式处理，Worker 完成当前操作后才会退出，因此可能需要等待一段时间。任务运行期间不要移动、替换或断开输入数据、网格文件及结果磁盘。

$\quad \quad$在“运行记录”页可以刷新历史作业、打开完整日志或打开作业记录目录。清空记录只会删除保存的作业描述、状态和日志，原始 GRMHD 数据、科研结果及图件保持不变。

$\quad \quad$科研输出位于所选结果根目录下的任务目录，例如 `analysis/`、`fast/`、`slow/`、`region_error/` 和 `benchmark/`。复制结果或等待任务完成后，可在“后处理”页点击“刷新”，让界面重新扫描状态与签名。

## 7. 常见问题

- **应用程序无法启动：** 从官方 Release 重新下载可执行文件，核对校验和，将其移到本地可写目录后重试。安全软件可能隔离未签名的科研程序。
- **没有发现数据帧：** 选择直接包含 `dataNNNN.dat` 的目录，并检查文件名、读取权限和文件是否复制完整。
- **网格或签名不匹配：** 选择与当前帧序列对应的 `grid_mks.in`，不要混用不同模拟的网格和快照。
- **慢光结果没有有效帧范围：** 在目标观者时刻两侧提供足够输入帧，或改用受当前数据支持的较窄时间窗口。
- **无法执行后处理比较：** 使用相同输入与模型签名完成兼容的快光和慢光任务，然后刷新结果列表。
- **任务失败或长时间没有进展：** 打开任务抽屉，或在“运行记录 > 查看日志”中读取完整日志；报告问题时请保留该日志。

$\quad \quad$报告可复现问题时，请提供 CoportSL 版本、Windows 版本、所选任务、相关参数和完整作业日志。请勿向公开 Issue 上传受限或未获授权的 GRMHD 数据。
